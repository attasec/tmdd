# =============================================================================
# TMDD System Definition
# =============================================================================
# This file defines the system being modeled.
# Edit the name and description to match your system.
# =============================================================================

system:
  name: "TMDD CLI"
  description: "Threat Modeling Driven Development - a Python CLI tool that manages YAML-based threat models, validates cross-references, generates AI prompts, and produces interactive HTML reports and diagrams"
  version: "0.5.1"

